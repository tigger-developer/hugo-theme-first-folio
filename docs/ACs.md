# Acceptance Criteria

Central acceptance-criteria record for closed issues.

- Cutover date: 2026-07-03
- Last migrated AC: AC23 from #70 on 2026-07-09

**Key:** ✅ passing · ⏳ pending · ❌ failing · ~~🚫 removed~~

| ID | Source | AC | Tests |
|---|---|---|---|
| AC1 | Issue #62 `AC62.1` | Given a consuming site defines audiobook content using the documented theme interface, First Folio renders the audiobook with a theme-owned layout and one playable chapter entry per chapter. | ✅ RT-62.1: Example site build contains an audiobook page for the demo content.<br>✅ RT-62.2: The generated audiobook page contains one audio control per configured chapter.<br>✅ RT-62.3: Each chapter entry exposes its configured title, media URL, and stable chapter identifier in the rendered page markup.<br>✅ RT-62.4: A fixture with audiobook content and no site-local audiobook template still renders through the First Folio theme-owned layout.<br>✅ RT-66.1: Same-bundle relative media source resolves to the page-resource URL in HTML.<br>✅ RT-66.3: `./` relative media source resolves to the page-resource URL in HTML.<br>✅ RT-66.4: Nested page-bundle media source resolves to the page-resource URL in HTML.<br>✅ RT-66.5: `../sibling-bundle/` media source resolves to the sibling page-resource URL in HTML. |
| AC2 | Issue #62 `AC62.2` | Given the example site contains the demo audiobook, First Folio serves repo-local demo audio files through the audiobook page and podcast feed. | ✅ RT-62.5: The example site serves each demo `.m4a` episode over HTTP from the public audio path with nonzero response body.<br>✅ RT-62.6: The generated audiobook page uses URLs for the copied repo-local demo audio files.<br>✅ RT-62.7: The generated podcast feed uses URLs for the copied repo-local demo audio files, and those enclosure paths resolve over HTTP.<br>✅ UT-62.1: Open the example site audiobook page from `make serve`, confirm it appears in main navigation, play a demo chapter, and confirm the page is instructive as a consuming-site example. |
| AC3 | Issue #62 `AC62.3` | Given an audiobook page has podcast feed support enabled, First Folio publishes an RSS 2.0 podcast feed for the same chapter set with the required channel and enclosure metadata. | ✅ RT-62.8: Example site build contains an audiobook podcast feed at the documented path.<br>✅ RT-62.9: XML-aware feed inspection shows one item per configured chapter.<br>✅ RT-62.10: Every feed item contains an enclosure with absolute URL, MIME type, and byte length.<br>✅ RT-62.11: XML-aware feed inspection shows required channel metadata, including title, description, site URL, language, and explicit-status metadata.<br>✅ RT-66.2: Same-bundle relative media source resolves to the page-resource URL in RSS.<br>✅ RT-66.6: Root-relative media sources keep valid HTML/RSS URLs.<br>✅ RT-66.7: Remote media sources are preserved in HTML/RSS.<br>✅ RT-66.9: Relative page-resource media can supply RSS enclosure length from the resolved resource. |
| AC4 | Issue #62 `AC62.4` | Given audiobook metadata is incomplete or malformed, First Folio fails the Hugo build with audiobook-specific template errors instead of publishing invalid page or feed data. | ✅ RT-62.12: Missing chapter media URL fails a fixture build with an audiobook-specific message.<br>✅ RT-62.13: Missing enclosure byte length fails a fixture build with an audiobook-specific message.<br>✅ RT-62.14: Missing chapter MIME type fails a fixture build with an audiobook-specific message.<br>✅ RT-62.15: Missing podcast title or description fails a fixture build with an audiobook-specific message.<br>✅ RT-62.16: Missing or duplicate stable chapter identifiers fail a fixture build with an audiobook-specific message.<br>✅ RT-66.8: Unresolved relative media source fails the Hugo build with an audiobook-specific diagnostic. |
| AC5 | Issue #62 `AC62.5` | Given a visitor uses an audiobook page in a browser, First Folio preserves each chapter's listening position locally and restores it on return visits in the same browser. | ~~🚫 RT-62.17: Browser-level player test stores playback position in localStorage with stable book/chapter keys.~~<br>~~🚫 RT-62.18: Browser-level player test restores stored playback position when a matching audio element loads.~~<br>~~🚫 RT-62.19: Browser-level player test ignores invalid or missing stored positions without preventing audio controls from loading.~~<br>✅ UT-62.2: In a real browser, open the example site audiobook page from `make serve`, play a chapter long enough to advance its timestamp, pause or refresh, reload the same page in the same browser, and confirm the chapter resumes from the stored position while invalid/missing stored positions do not block audio controls. |
| AC6 | Issue #62 `AC62.6` | Given a consuming site needs crawler-discouragement metadata, First Folio exposes generic noindex support for audiobook pages without imposing access control or site-specific robots paths. | ✅ RT-62.20: An audiobook fixture with noindex enabled includes `noindex`, `nofollow`, and `noarchive` robots metadata.<br>✅ RT-62.21: An audiobook fixture without noindex enabled omits audiobook-specific robots metadata.<br>~~🚫 RT-62.22: Theme documentation states that robots metadata is advisory and consuming sites own `robots.txt`, private paths, and access control.~~ |
| AC7 | Issue #62 `AC62.7` | ~~🚫 Given First Folio's documented theme interface, consuming sites have enough documentation and example content to reproduce audiobook pages and feeds without copying templates.~~ | ~~🚫 RT-62.23: Theme documentation describes required audiobook metadata, optional metadata, feed path/output configuration, copied demo-audio pattern, and local player behaviour.~~<br>~~🚫 RT-62.24: The example site documents or demonstrates the minimal audiobook frontmatter/data shape used by the theme-owned layout.~~<br>~~🚫 RT-62.25: Existing non-audiobook example pages keep their current default single-page rendering.~~<br>~~🚫 UT-62.3: A human reviews the example site demo and documentation for clarity as a consuming-project guide.~~ |
| AC8 | Issue #62 `AC62.8` | Given existing non-audiobook example pages, First Folio keeps their default single-page rendering. | ✅ RT-62.25: Existing non-audiobook example pages keep their current default single-page rendering. |
| AC9 | Issue #64 `AC64.1` | Given generated media metadata exists in `hugo.Data` keyed by audiobook/podcast id plus item id, the audiobook RSS template uses it for item duration. | ✅ RT-64.1: Fixture with generated metadata emits matching `itunes:duration` values for RSS items. |
| AC10 | Issue #64 `AC64.2` | Given an item references a local static audio file, the RSS enclosure length can be derived from the actual file size instead of requiring `byteLength` in front matter. | ✅ RT-64.2: Fixture omits `byteLength` but generated feed enclosure length matches the fixture audio file size. |
| AC11 | Issue #64 `AC64.3` | Given both generated metadata and front matter values exist for binary-derived media facts, generated metadata takes precedence for those binary-derived facts. | ✅ RT-64.3: Fixture with conflicting front matter and generated values emits generated `duration` and `byteLength` values. |
| AC12 | Issue #64 `AC64.4` | Given generated metadata mirrors an item `src`, First Folio treats a mismatch with front matter `src` as stale generated data and fails clearly. | ✅ RT-64.4: Fixture with mismatched generated/front matter `src` fails the Hugo build with an audiobook/podcast media metadata diagnostic. |
| AC13 | Issue #64 `AC64.5` | Given no generated metadata exists, existing front matter based audiobook pages continue to build. | ✅ RT-64.5: Existing audiobook fixture with front matter `byteLength` and `duration` still emits a valid feed. |
| AC14 | Issue #64 `AC64.6` | Given media is remote or otherwise unavailable to Hugo locally, manual front matter or generated metadata can still satisfy RSS enclosure metadata requirements. | ✅ RT-64.6: Fixture with remote media URL and front matter `byteLength` emits a valid enclosure length. |
| AC15 | Issue #64 `AC64.7` | Given required podcast enclosure length cannot be resolved from generated metadata, local file stat, or front matter, the feed build fails with a clear diagnostic. | ✅ RT-64.7: Fixture missing resolvable enclosure length fails the Hugo build with an audiobook/podcast media metadata diagnostic. |
| AC16 | Issue #64 `AC64.8` | Given a site maintainer wants reproducible podcast/audiobook builds, theme docs explain the generated data shape, precedence rules, remote-media fallback, local file-size fallback, and the recommendation to generate duration outside the theme. | ✅ UT-64.1: Human documentation review approved by `APPROVED 64`. |
| AC17 | Issue #64 `AC64.9` | Given the exampleSite is deployed from GitHub Pages, the production build uses committed example audiobook media metadata, CI does not run ffprobe, and the workflow supplies the Hugo environment explicitly. | ✅ RT-64.8: `make generate-audiobook-metadata` writes generated media metadata for the exampleSite audiobook demo.<br>✅ RT-64.9: Pages workflow uses official checkout, verifies committed metadata, runs the exampleSite production build in a pinned Hugo container, supplies `HUGO_ENVIRONMENT`, and does not install or run `ffprobe` during deploy.<br>✅ RT-64.13: `make build` fails when committed exampleSite media metadata is missing. |
| AC18 | Issue #70 `AC70.1` | Given an audiobook or podcast page, First Folio presents one coherent web-listening surface for the whole audio work while preserving visible, tappable chapter or episode granularity. | ✅ RT-70.1: Generated audiobook HTML contains a single theme-owned player container with active-item metadata, one visible play/pause control, seek controls, optional previous/next controls, and one ordered visible tappable chapter selector.<br>✅ RT-70.2: Generated podcast HTML uses the same player structure with podcast/episode labels and a visible tappable episode selector.<br>✅ UT-70.1: Human review confirms the player reads as one listening experience while chapter/episode names remain easy to scan and tap. |
| AC19 | Issue #70 `AC70.2` | Given a listener uses the web player, existing local position storage, one-active-item playback, and automatic next-item progression remain available. | ✅ RT-70.3: Player script preserves per-book/per-item localStorage keys and restores saved item positions.<br>✅ RT-70.4: Player script pauses other audio items when a new item starts.<br>✅ RT-70.5: Player script starts the next item in order when the current item ends without skipping the beginning. |
| AC20 | Issue #70 `AC70.3` | Given a page publishes a podcast RSS output, feed-subscription controls live inside a compact sidebar disclosure rather than the main listening surface and avoid named app links by default. | ✅ RT-70.6: Generated audio page places all podcast feed controls inside a sidebar `details` disclosure or equivalent compact help panel.<br>✅ RT-70.7: Default generated subscription controls contain no Apple Podcasts, Overcast, Castro, or AntennaPod named app links.<br>✅ RT-70.8: The generated main player area contains no RSS feed link, feed-copy button, or podcast-app setup text.<br>✅ RT-70.18: Mobile audio pages collapse secondary listening panels while desktop audio pages keep them expanded.<br>✅ UT-70.2: Human review confirms the subscription panel feels secondary to the player without hiding the RSS option from technical users. |
| AC21 | Issue #70 `AC70.4` | Given podcast-app setup is available, First Folio provides short manual-feed instructions that use the word "Link", keep the feed link wholly inside the disclosure, and provide copy feedback. | ✅ RT-70.9: Generated subscription panel includes the absolute podcast feed link as copyable data and a visible feed link inside the disclosure only.<br>✅ RT-70.10: Generated subscription panel uses configurable manual setup copy with user-facing "Link" wording and no default "URL" wording.<br>✅ RT-70.11: Player script copies the podcast feed link when the feed panel or copy control is activated and exposes "Copied Podcast Feed Link" feedback.<br>✅ UT-70.3: Human review confirms the default instructions are brief, non-intimidating, and honest about platform limits. |
| AC22 | Issue #70 `AC70.5` | Given a visitor wants quick return access, First Folio provides sidebar affordances for sharing or saving the page above Home Screen guidance without claiming a nonexistent web bookmark API. | ✅ RT-70.12: Generated audio page includes a compact share/save sidebar panel with a page link copy control and optional Web Share trigger metadata.<br>✅ RT-70.13: Generated audio page includes Home Screen guidance below the share/save panel and includes manifest/icon metadata when the feature is enabled.<br>✅ RT-70.14: Player script exposes iOS-specific and Android-specific Home Screen instructions when platform detection identifies those families, while preserving generic fallback guidance.<br>✅ RT-70.17: Browsers without native Web Share copy the page link and show feedback instead of hiding the Home Screen share control.<br>✅ UT-70.4: Human review confirms the sidebar guidance distinguishes sharing/bookmarking, Home Screen saving, and podcast RSS setup. |
| AC23 | Issue #70 `AC70.6` | Given First Folio documentation and examples, podcast and audiobook pages share the same listening UX with differences limited to labels, feed type, ordering semantics, and example content. | ✅ RT-70.15: Example-site audiobook and podcast pages both use the unified player/sidebar structure.<br>✅ RT-70.16: Documentation describes the shared UX contract, text override contract, platform limits, and the intentional podcast/audiobook differences.<br>✅ UT-70.5: Human review confirms the example pages demonstrate podcast and audiobook modes without divergent or confusing interaction patterns. |
