# Acceptance Criteria

Central acceptance-criteria record for closed issues.

- Cutover date: 2026-07-03
- Last migrated AC: AC8

**Key:** ✅ passing · ⏳ pending · ❌ failing · ~~🚫 removed~~

| ID | Source | AC | Tests |
|---|---|---|---|
| AC1 | Issue #62 `AC62.1` | Given a consuming site defines audiobook content using the documented theme interface, First Folio renders the audiobook with a theme-owned layout and one playable chapter entry per chapter. | ✅ RT-62.1: Example site build contains an audiobook page for the demo content.<br>✅ RT-62.2: The generated audiobook page contains one audio control per configured chapter.<br>✅ RT-62.3: Each chapter entry exposes its configured title, media URL, and stable chapter identifier in the rendered page markup.<br>✅ RT-62.4: A fixture with audiobook content and no site-local audiobook template still renders through the First Folio theme-owned layout. |
| AC2 | Issue #62 `AC62.2` | Given the example site contains the demo audiobook, First Folio serves repo-local demo audio files through the audiobook page and podcast feed. | ✅ RT-62.5: The example site serves each demo `.m4a` episode over HTTP from the public audio path with nonzero response body.<br>✅ RT-62.6: The generated audiobook page uses URLs for the copied repo-local demo audio files.<br>✅ RT-62.7: The generated podcast feed uses URLs for the copied repo-local demo audio files, and those enclosure paths resolve over HTTP.<br>✅ UT-62.1: Open the example site audiobook page from `make serve`, confirm it appears in main navigation, play a demo chapter, and confirm the page is instructive as a consuming-site example. |
| AC3 | Issue #62 `AC62.3` | Given an audiobook page has podcast feed support enabled, First Folio publishes an RSS 2.0 podcast feed for the same chapter set with the required channel and enclosure metadata. | ✅ RT-62.8: Example site build contains an audiobook podcast feed at the documented path.<br>✅ RT-62.9: XML-aware feed inspection shows one item per configured chapter.<br>✅ RT-62.10: Every feed item contains an enclosure with absolute URL, MIME type, and byte length.<br>✅ RT-62.11: XML-aware feed inspection shows required channel metadata, including title, description, site URL, language, and explicit-status metadata. |
| AC4 | Issue #62 `AC62.4` | Given audiobook metadata is incomplete or malformed, First Folio fails the Hugo build with audiobook-specific template errors instead of publishing invalid page or feed data. | ✅ RT-62.12: Missing chapter media URL fails a fixture build with an audiobook-specific message.<br>✅ RT-62.13: Missing enclosure byte length fails a fixture build with an audiobook-specific message.<br>✅ RT-62.14: Missing chapter MIME type fails a fixture build with an audiobook-specific message.<br>✅ RT-62.15: Missing podcast title or description fails a fixture build with an audiobook-specific message.<br>✅ RT-62.16: Missing or duplicate stable chapter identifiers fail a fixture build with an audiobook-specific message. |
| AC5 | Issue #62 `AC62.5` | Given a visitor uses an audiobook page in a browser, First Folio preserves each chapter's listening position locally and restores it on return visits in the same browser. | ~~🚫 RT-62.17: Browser-level player test stores playback position in localStorage with stable book/chapter keys.~~<br>~~🚫 RT-62.18: Browser-level player test restores stored playback position when a matching audio element loads.~~<br>~~🚫 RT-62.19: Browser-level player test ignores invalid or missing stored positions without preventing audio controls from loading.~~<br>✅ UT-62.2: In a real browser, open the example site audiobook page from `make serve`, play a chapter long enough to advance its timestamp, pause or refresh, reload the same page in the same browser, and confirm the chapter resumes from the stored position while invalid/missing stored positions do not block audio controls. |
| AC6 | Issue #62 `AC62.6` | Given a consuming site needs crawler-discouragement metadata, First Folio exposes generic noindex support for audiobook pages without imposing access control or site-specific robots paths. | ✅ RT-62.20: An audiobook fixture with noindex enabled includes `noindex`, `nofollow`, and `noarchive` robots metadata.<br>✅ RT-62.21: An audiobook fixture without noindex enabled omits audiobook-specific robots metadata.<br>~~🚫 RT-62.22: Theme documentation states that robots metadata is advisory and consuming sites own `robots.txt`, private paths, and access control.~~ |
| AC7 | Issue #62 `AC62.7` | ~~🚫 Given First Folio's documented theme interface, consuming sites have enough documentation and example content to reproduce audiobook pages and feeds without copying templates.~~ | ~~🚫 RT-62.23: Theme documentation describes required audiobook metadata, optional metadata, feed path/output configuration, copied demo-audio pattern, and local player behaviour.~~<br>~~🚫 RT-62.24: The example site documents or demonstrates the minimal audiobook frontmatter/data shape used by the theme-owned layout.~~<br>~~🚫 RT-62.25: Existing non-audiobook example pages keep their current default single-page rendering.~~<br>~~🚫 UT-62.3: A human reviews the example site demo and documentation for clarity as a consuming-project guide.~~ |
| AC8 | Issue #62 `AC62.8` | Given existing non-audiobook example pages, First Folio keeps their default single-page rendering. | ✅ RT-62.25: Existing non-audiobook example pages keep their current default single-page rendering. |
